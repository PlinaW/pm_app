# Read about fixtures at https://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

# This model initially had no columns defined. If you add columns to the
# model remove the "{}" from the fixture names and add the columns immediately
# below each fixture, per the syntax in the comments below
#
app_access:
  name: Umożliwienie dostępu do apki
  goal: Możliwość założenia konta przez użytkownika
  start_date: <%= Date.today.strftime("%Y-%m-%d") %>
  end_date: <%= (Date.today + 4.weeks).strftime("%Y-%m-%d")%>

search_courier:
  name: Wyszukiwarka kurierów
  goal: Możlwość wyszukania i przypisania kuriera do zlecenia
  start_date: <%= Date.today.strftime("%Y-%m-%d") %>
  end_date: <%= (Date.today + 4.weeks).strftime("%Y-%m-%d")%>